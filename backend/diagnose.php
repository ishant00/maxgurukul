<?php require_once __DIR__.'/auth.php';api_auth();header('Content-Type: application/json');$files=['cms_storage.json','enquiries.json','media_map.json','disclosure_data.json'];$checks=[];foreach($files as $f){$p=__DIR__.'/'.$f;$checks[$f]=['exists'=>file_exists($p),'writable'=>is_writable(__DIR__)];}echo json_encode(['php'=>PHP_VERSION,'session'=>session_save_path(),'authenticated'=>true,'checks'=>$checks,'uploads_writable'=>is_writable(__DIR__.'/uploads')]);
